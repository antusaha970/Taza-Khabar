import React from 'react';

const style = {
    color: 'white',
    background: "black",
    padding: "10px",
    textAlign: "center",
}

const Foot = () => {
    return (
        <div style={style}>
            <small>&copy; News org</small>
        </div>
    );
};

export default Foot;